version: '3.8'
services:
  app:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - ./uploads:/app/uploads
      - ./server/data.sqlite:/app/server/data.sqlite
    environment:
      - PORT=3000
      - JWT_SECRET=change-me
    restart: unless-stopped
